{
  "name": "unreal-open-mcp",
  "version": "0.0.1",
  "description": "MCP server (stdio) routing tool calls to the live Unreal Open MCP bridge.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "unreal-open-mcp": "dist/index.js"
  },
  "files": [
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "build:test": "tsc -p tsconfig.test.json",
    "typecheck": "tsc --noEmit",
    "test": "npm run build:test && node --test 'dist-test/**/*.test.js'",
    "smoke:p1": "npm run build && node scripts/p1-parity-smoke.mjs",
    "smoke:p2": "npm run build && node scripts/p2-parity-smoke.mjs",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "unreal",
    "unreal-engine",
    "ai",
    "agent",
    "claude",
    "cursor",
    "stdio"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "~5.6.2"
  },
  "license": "MIT",
  "author": "Unreal Open MCP",
  "homepage": "https://github.com/AlexeyPerov/Unreal-Open-MCP",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/AlexeyPerov/Unreal-Open-MCP.git"
  },
  "bugs": {
    "url": "https://github.com/AlexeyPerov/Unreal-Open-MCP/issues"
  }
}
