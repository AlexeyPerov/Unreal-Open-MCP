# API and Protocol Surfaces

This file is the index for external interfaces and protocol contracts exposed by this repository.

## Domain reference

| Document | Covers | Status |
|---|---|---|
| `api/bridge-http.md` | Unreal bridge HTTP endpoints (`/ping`, `/tools/*`), envelopes, and errors. | `/ping` + `POST /tools/{name}` dispatch shipped; typed tools continue to land |
| `api/mcp-tools.md` | MCP tool catalog, tool families, route policy (live/offline/local/batch), `capabilities` surface, rule + fix catalog contract. | `ping` + actor family (`actor_find`, `actor_create`, `actor_modify`, `object_modify`, `actor_set_parent`, `actor_duplicate`, `actor_destroy`, `actor_component_add`/`_destroy`/`_get`/`_modify`/`_list_all`) + level family (`level_open`, `level_save`, `level_list_loaded`, `level_set_current`, `level_unload_sublevel`, `level_get_data`, `level_create`) + gate meta-tools (`validate_edit`, `checkpoint_create`, `delta`) + `apply_fix` + `capabilities` (local-route; discovers tools + verify rules + fixes in one call) + asset read family (`asset_find`, `asset_get_data`) + Content Browser CRUD family (`asset_create_folder`, `asset_copy`, `asset_move`, `asset_delete`, `asset_refresh`) + material family (`material_create`, `material_modify`, `material_get_data`) + asset import (`asset_import` — absolute host file → `/Game` folder via AssetTools/Interchange; supported types: textures PNG/JPG/TGA/EXR, static meshes FBX/OBJ/glTF, sounds WAV; `replace_existing` / `save` default false) + editor family (`editor_application_get_state` / `editor_application_set_state` — read-only PIE snapshot { isPlaying, isPaused, isSimulating, editorMap } + start/stop/pause/resume driver; ALL four transitions are latent (return `{ pending:true }`, poll get-state); pause/resume use `GEditor->SetPIEWorldsPaused` + the `bDebugPauseExecution` flag; `editor_selection_get` / `editor_selection_set` — read/drive actor selection as P2 identity refs, replace-by-refs or explicit `clear`, resolve-before-mutate, refuse-empty; set tools mutating + gated) + console family (`console_get_logs` / `console_clear_logs` — bounded GLog ring buffer read with verbosity/category/substring filters + `console_run_command` — GEngine->Exec, mutating + gated, destructive) + reflection family (`reflection_method_find` — UFunction discovery + `reflection_method_call` — safety-gated ProcessEvent, BlueprintCallable/CallInEditor only, target XOR class/CDO, mutating + gated) + screenshot family (`screenshot_viewport` / `screenshot_game_view` / `screenshot_camera` / `screenshot_isolated` — four read-only image-capture tools returning a base64 PNG as MCP image content via a top-level bridge `image` envelope field; dimension-capped [1, 2048], opaque-PNG, 40 MiB byte cap; viewport reads `FViewport::ReadPixels`, game-view requires PIE else `pie_not_running`, camera/isolated render via transient SceneCapture2D cleaned up on every path) + `bridge_status` (operator / recovery-oriented health snapshot — composes the instance-lock classifier with one `/ping` probe into a coarse `status` token `running` / `compiling` / `stopped` / `unreachable` / `dead_bridge`; local-route, read-only, gate-free; a dead/stopped bridge is a successful status read, never an error; `recoveryHint` points at `console_get_logs` for `dead_bridge` as the interim offline surface) + Blueprint family (`blueprint_create` — new Blueprint class from a resolvable parent via `FKismetEditorUtilities::CreateBlueprint`; `path` is the `/Game` package path (object-path form also accepted and normalised), `parent_class` defaults to Actor, any Blueprintable parent allowed when `CanCreateBlueprintOfClass` passes; in-session registration (no disk save); any-`UObject` collision probe so a non-Blueprint asset at the target path is a structured `asset_already_exists` (never a fatal assert); mutating, gate Enforce, `paths_hint` required; `blueprint_get` — read-only scoped graph summary for inspection `{ name, path, parentClass, variables[], components[], functions[], events[] (with `enabled`), interfaces[], parentChain[] }`; events carry `enabled` so a fresh Actor Blueprint's DISABLED ghost event nodes are distinguishable from enabled ones; `blueprint_add_component` — add an SCS component node via the public Simple Construction Script surface (`CreateNode` + `AddChildNode` under an optional scene-component `parent_component`, or `AddNode` for a root); guards reject non-`UActorComponent` classes (`invalid_component_class`), abstract/deprecated classes (`abstract_component` — `CreateNode` -> `NewObject` would fatally assert, e.g. `LightComponentBase`), name collisions across the SCS ↔ member variables ↔ parent-class properties (`name_collision`), a missing parent (`parent_not_found`), and non-`USceneComponent` attachment (`invalid_attachment`); `blueprint_remove_component` — delete an SCS node by variable name via `RemoveNodeAndPromoteChildren` (children re-parented onto the removed node's parent so a subtree is never orphaned); both mutate the asset SCS, mark the Blueprint structurally modified, gate Enforce, `paths_hint` required; `blueprint_add_variable` — add a typed member variable via `FBlueprintEditorUtils::AddMemberVariable` using the §3.2 pin-type forward map (`type` token: primitive `bool`/`int`/`int64`/`byte`/`float`/`string`/`name`/`text`, math struct `vector`/`vector2d`/`rotator`/`transform`/`color`, or a resolvable object/struct path; optional `is_array` + `default_value` in UE text format — the default only takes effect after a compile); guards reject cross-namespace name collisions (member vars ↔ SCS components ↔ parent properties → `name_collision`) and unresolvable type tokens (`invalid_type`); `blueprint_modify_variable` — rename and/or retype an existing member variable with validate-before-mutate ordering (the rename is validated — well-formed + no collision — BEFORE the retype commits, so a colliding/ill-formed `new_name` never leaves a partial retype); at least one of `new_name`/`new_type` required; `blueprint_set_default` — write a Class Default Object property via the property's own text importer (`ImportText_Direct`) bracketed with `Pre`/`PostEditChangeProperty`; changes the CLASS DEFAULT so it affects newly-spawned instances only, not placed actors; compile-first — a member variable added via `blueprint_add_variable` is NOT a property on the generated class until `blueprint_compile` lands it, so `set_default` on such a property reports `property_not_found` with a message pointing at `blueprint_compile`; all three mutate the asset, gate Enforce, `paths_hint` required; `blueprint_add_function` / `blueprint_add_event` (P6.4 — Blueprint function / event graph STUBS: add_function creates an empty user function-graph stub via `FBlueprintEditorUtils::CreateNewGraph` + `AddFunctionGraph` — the K2 schema auto-wires the entry/result nodes, the MVP stub is parameter-less; guards reject the outer-name hijack — `CreateNewGraph` would otherwise resolve a name clash by renaming the EXISTING object aside (the EventGraph or any graph outered to the Blueprint) and silently report success, so a pre-check on `FunctionGraphs` + any `UObject` outered to the Blueprint surfaces `name_collision` instead — and a `create_graph_failed` on null return; add_event enables or creates an overridable parent event node (e.g. `ReceiveBeginPlay` / `ReceiveTick`) via the K2 schema's `FunctionCanBePlacedAsEvent` + `FKismetEditorUtilities::AddDefaultEventNode` — the two-pronged resolution mirrors the K2 editor: a fresh Actor event graph is pre-seeded with DISABLED ghost nodes for the common events (those ghosts are INERT — the event does NOT fire until enabled), so an existing disabled ghost is exactly the node the tool ENABLES (enabling the ghost IS the "add event" op — never a false no-op), an already-enabled node is a real duplicate (`event_already_exists`), and no existing node → `AddDefaultEventNode` mints a fresh one the tool enables; a `name` that names no parent `UFunction` OR names a non-`BlueprintEvent` function (e.g. `K2_DestroyActor` is `BlueprintCallable` but not a `BlueprintEvent`) is rejected by the schema check with `not_an_event`; STUB-ONLY scope — body authoring (`add_node` / `connect_pins` / free-form wiring) is OUT OF SCOPE for both tools; both mutate the asset, mark the Blueprint structurally modified, gate Enforce, `paths_hint` required) + `blueprint_compile` (P6.5 — Blueprint compile: compiles a Blueprint via `FKismetEditorUtilities::CompileBlueprint` with a silent `FCompilerResultsLog` and returns a STRUCTURED error/warning list — the AI feedback loop; result `{ succeeded, numErrors, numWarnings, messages[] }` where each message is `{ severity ('error'\|'warning'\|'info'), message, node?, graph? }` with best-effort node/graph attribution from the compiler's UObject tokens; a FAILED compile is a NORMAL, expected result, NOT a transport failure — the envelope stays `ok:true` and the result carries `succeeded:false` + the populated `messages[]` so an agent reads the diagnostics, fixes via the structure-edit tools, and recompiles; only tool-level errors (missing path / missing asset / reserved root / malformed body) map to `ok:false`; mutating, gate Enforce, `paths_hint` required) + Source read/list family (`source_read` — read a project C++ source file under `<Project>/Source/` with an optional 1-based `start_line`/`end_line` window and a soft `max_lines` cap (default 2000, ceiling 20000); returns `{ path, total_lines, start_line, end_line, truncated, lines:[{line,text}] }`; read-only, gate-free; `source_list` — enumerate source files under `<Project>/Source/`, optionally scoped to a `module` folder, with `recursive` (default true) + an `extensions` allow-list (default `.h/.hpp/.c/.cc/.cpp/.cs`); returns `{ root, files:[{path,bytes}], count, total_bytes }`; read-only, gate-free; both JAILED to `<Project>/Source/` via `ResolveJailedPath` — `..`, absolute-outside, and NTFS ADS (`:`) escapes return `path_escapes_jail` and never read; the jail targets the PROJECT `Source/`, not `Plugins/UnrealOpenMCP`) + Source CRUD family (`source_create_class` — scaffold a header + cpp from parent-kind templates (UObject default / Actor / ActorComponent / None) into an existing module folder; `class_name` is the bare name without the U/A/F prefix (the prefix is derived from `parent_class`); emits the `<MODULE>_API` dllexport macro + `GENERATED_BODY()`; refuses overwrite unless `force:true`; a failed cpp write rolls back the header; no Build.cs / module authoring (the module folder must already exist); mutating, gate Enforce, `paths_hint` required; `source_update` — full-file replace or a 1-based inclusive `start_line`/`end_line` line-range splice on an existing file (half-range rejected with `invalid_parameter`); preserves the detected EOL (CRLF/LF) and trailing newline when splicing so a one-line splice is a one-line diff, not a whole-file EOL rewrite; validates the range in int64 before narrowing (out-of-range → `invalid_line_range`, never a silent mis-edit); writes are BOM-less UTF-8; mutating, gate Enforce, `paths_hint` required; `source_delete` — delete a single file (refuses directories with `is_directory`, refuses missing with `file_not_found`); destructive and NOT undoable from MCP (no `FScopedTransaction` / recycle bin — source files are not UObject assets; the gate checkpoint is the only safety net); mutating, gate Enforce, `paths_hint` required; all three JAILED to `<Project>/Source/` — escapes never write; no auto-compile — an agent calls the compile tool after the scaffold + edits) shipped; rest TBD |
| `api/resources.md` | MCP resource URIs, payload shapes, and resource router behavior. | TBD |

## Contract boundaries

- Bridge HTTP contract source: `packages/bridge/Source/UnrealOpenMcpEditor/Private/Bridge/` (`GET /ping` + `POST /tools/{name}` dispatch with the `{ok,result,error}` envelope shipped; mutating dispatches widen the envelope with a `gate` summary block per [Gate policy](api/bridge-http.md#gate-policy))
- Gate policy source: `packages/bridge/Source/UnrealOpenMcpEditor/Private/Gate/` (`UnrealOpenMcpGatePolicy` wraps every mutating dispatch in `checkpoint → mutate → validate → delta`; `UnrealOpenMcpVerifyGateAdapter` is the bridge→verify glue with the rule-selection + filter surface the meta-tools consult; `UnrealOpenMcpCheckpointStore` mirrors the gate's checkpoint for the meta-tools)
- Gate meta-tools source: `packages/bridge/Source/UnrealOpenMcpEditor/Private/MetaTools/UnrealOpenMcpGateMetaTools.h` (the three read-only tools `validate_edit` / `checkpoint_create` / `delta` — the explicit checkpoint → mutate → delta surface; bypass `GatePolicy.Execute` so they do not recurse)
- apply_fix source: `packages/bridge/Source/UnrealOpenMcpEditor/Private/MetaTools/UnrealOpenMcpApplyFixTool.h` (the fix-application workflow — dry-run preview lists the description / safe flag; non-dry-run applies run through the gate runner with a `FixRollback` snapshot so a corrupting fix is auto-reverted on failure or new errors under `enforce`; v1 ships the single Safe provider `clear_broken_soft_reference`)
- Instance lock + port resolver source: `packages/bridge/Source/UnrealOpenMcpRuntime/Public/Bridge/UnrealOpenMcpInstancePortResolver.h` (deterministic port + lock path), `packages/bridge/Source/UnrealOpenMcpEditor/Private/Bridge/UnrealOpenMcpBridgeInstanceLock.h` (lock file writer)
- SHA-256 source: `packages/bridge/Source/UnrealOpenMcpRuntime/Public/Crypto/UnrealOpenMcpSha256.h` (self-contained FIPS 180-4; byte-for-byte parity with Node `crypto.createHash('sha256')`)
- MCP server routing/registry source: `mcp-server/src/index.ts` (stdio server + tool dispatch; local-route tools `capabilities` and `bridge_status` resolve in-process before the live router is consulted), `mcp-server/src/live-client.ts` (live bridge HTTP client; routes `ping` → `GET /ping` and every other live tool → `POST /tools/{name}`)
- MCP capabilities surface (local rule/fix catalog + builder): `mcp-server/src/capabilities/` (shipped — `rule-catalog.ts` mirrors the implemented verify rules and fix providers; `build-capabilities.ts` assembles the capabilities payload; `unreal_open_mcp_capabilities` is local-route, no bridge hop)
- MCP tool definitions source: `mcp-server/src/tools/` (`ping` + the actor family shipped: `actor_find`, `actor_create`, `actor_modify`, `object_modify`, `actor_set_parent`, `actor_duplicate`, `actor_destroy`, and the five `actor_component_*` tools; plus the level family shipped: `level_open`, `level_save`, `level_list_loaded`, `level_set_current`, `level_unload_sublevel`, `level_get_data`, `level_create`; plus the gate meta-tools shipped: `validate_edit`, `checkpoint_create`, `delta`; plus `apply_fix`; plus `capabilities` (local-route discovery); plus the asset read family shipped: `asset_find`, `asset_get_data`; plus the Content Browser CRUD family shipped: `asset_create_folder`, `asset_copy`, `asset_move`, `asset_delete`, `asset_refresh`; plus the material family shipped: `material_create`, `material_modify`, `material_get_data`; plus asset import shipped: `asset_import` (absolute host file → `/Game` folder via `IAssetTools::ImportAssetTasks`; supported types depend on installed importers — textures/static meshes/sounds; `replace_existing` / `save` default false); plus the editor family shipped: `editor_application_get_state` / `editor_application_set_state` (P5.1 — PIE get/set: read-only `{ isPlaying, isPaused, isSimulating, editorMap }` snapshot + start/stop/pause/resume driver; ALL four transitions are latent (return `{ pending:true }`, agent polls get-state) — start/stop via `RequestPlaySession`/`RequestEndPlayMap`, pause/resume via `GEditor->SetPIEWorldsPaused` + the `bDebugPauseExecution` flag; invalid transitions return `invalid_transition`; set-state mutating, gate Enforce, `paths_hint` required) and `editor_selection_get` / `editor_selection_set` (P5.2 — actor selection get/set: get returns `{ count, actors: IdentityRef[] }`; set replaces by refs or clears with explicit `clear:true`, resolve-before-mutate aborts on a bad ref, empty call refused; set mutating, gate Enforce, `paths_hint` required); plus the console family shipped: `console_get_logs` / `console_clear_logs` / `console_run_command` (P5.3 — bounded GLog ring-buffer collector: get is a filtered read (verbosity min-severity / category / substring + limit) over a session-scoped recent-N window (NOT a full Output Log mirror; cold-start gap + overflow documented), clear empties the buffer (read-only; buffer-local), run-command runs `GEngine->Exec` and returns `{ command, output, handled }` — mutating, gate Enforce, `paths_hint` required, destructive accepted-risk) and the reflection family shipped: `reflection_method_find` / `reflection_method_call` (P5.4 — first-party UFunction reflection, no ReflectorNet: find lists UFunctions with signature/flag descriptors + matched/returned honesty; call invokes by `method` on `target` XOR `class` (CDO) with a safety allow-list (BlueprintCallable / CallInEditor only; CDO path requires static / CallInEditor) — mutating, gate Enforce, `paths_hint` required, same accepted-risk class as run-command); plus the screenshot family shipped: `screenshot_viewport` / `screenshot_game_view` / `screenshot_camera` / `screenshot_isolated` (four read-only image-capture tools returning a base64 PNG as MCP image content — the bridge emits a top-level `image: { data, mediaType }` envelope field alongside `result`, and `mcp-server/src/live-client.ts` unwraps it into an MCP image content block so the base64 never surfaces as text; viewport captures `GEditor->GetActiveViewport()` via `FViewport::ReadPixels`; game-view requires a PIE session else `pie_not_running` (checked before the GPU guard, headless-testable); camera renders from a resolved camera actor through a transient `ASceneCapture2D` at the camera transform/FOV; isolated renders a single actor against a neutral background via a show-only list (target + recursively-attached children) + HDR-over-background composite, auto-framed by the actor's bounds; all four share dimension caps [1, 2048] per side (default 1024), force opaque alpha (transparent PNGs are useless to agents), and a 40 MiB encoded byte cap (`image_too_large` on exceed); transient capture actors + render targets are cleaned up on every return path (`ON_SCOPE_EXIT` destroy + `FGCObjectScopeGuard`); read-only, gate-free — reads pixels, mutates no editor/project state; requires a GPU-backed (windowed) editor else `rendering_unavailable`); plus `bridge_status` shipped (operator / recovery-oriented health snapshot — composes the instance-lock classifier (`instance-discovery.ts#classifyInstance`) with one `/ping` probe into a coarse `status` token: `running` / `compiling` / `stopped` / `unreachable` / `dead_bridge`; returns `classification`, `ping` summary, `instance` lock summary, `recoveryHint` (non-null only for `dead_bridge`, pointing at `console_get_logs` as the interim offline Output-Log surface — the dedicated offline compile-error reader is deferred), and a human-readable `nextStep`; local-route — resolved in-process by `handleLocalTool` with no bridge POST, read-only, gate-free, never spawns the editor; a dead/stopped bridge is a successful status read, never an error); plus the Blueprint family shipped: `blueprint_create` / `blueprint_get` (P6.1 — Blueprint create/get: create makes a new Blueprint class from a resolvable parent via `FKismetEditorUtilities::CreateBlueprint` (`path` = `/Game` package path, object-path form also accepted and normalised; `parent_class` defaults to `/Script/Engine.Actor`, any Blueprintable parent allowed when `CanCreateBlueprintOfClass` passes; in-session registration via `AssetRegistry.AssetCreated` + `MarkPackageDirty`, no disk save; the any-`UObject` collision probe turns a non-Blueprint asset at the target path into a structured `asset_already_exists` instead of the engine's fatal "same fully-qualified name, different class" allocation check; on a failed create the empty package is `MarkAsGarbage`'d; mutating, gate Enforce, `paths_hint` required; get is a read-only scoped graph summary `{ name, path, parentClass, variables: [{ name, type, isArray, default? }], components: [{ name, class, parent }], functions: [{ name, nodeCount }], events: [{ name, enabled }], interfaces: [classPath], parentChain: [className] }` — events carry `enabled` so a fresh Actor Blueprint's pre-seeded DISABLED ghost event nodes (ReceiveBeginPlay/ReceiveTick) are distinguishable from enabled ones; the shared helpers `ResolveBlueprint`, path normalization, name well-formedness, `BlueprintRef` JSON, and the pin-type reverse map are the spine every later Blueprint sub-tool builds on) and `blueprint_add_component` / `blueprint_remove_component` (P6.2 — Blueprint SCS components: add creates an SCS node via the public Simple Construction Script surface (`USimpleConstructionScript::CreateNode` + `AddChildNode` under an optional scene-component `parent_component`, or `AddNode` for a root); the component class must be a concrete `UActorComponent` subclass — `CreateNode` -> `NewObject` fatally asserts on an abstract/deprecated class (e.g. `/Script/Engine.LightComponentBase`), so the `CLASS_Abstract | CLASS_Deprecated | CLASS_NewerVersionExists` guard runs BEFORE `CreateNode` and returns `abstract_component`; the variable name is checked across the SCS, the `NewVariables` list, and the parent class's properties (`name_collision`) because those namespaces share the generated class's property namespace and would otherwise only fail at compile; attachment is a scene-graph op, so both the new component and its parent must be `USceneComponent`s (`invalid_attachment`); `MarkBlueprintAsStructurallyModified` follows so a later compile rebuilds the CDO; remove deletes a node via `RemoveNodeAndPromoteChildren` (children re-parented onto the removed node's parent so a subtree is never orphaned); both mutate the asset SCS, gate Enforce, `paths_hint` required) and `blueprint_add_variable` / `blueprint_modify_variable` / `blueprint_set_default` (P6.3 — Blueprint member variables + CDO defaults: add_variable creates a typed member variable via `FBlueprintEditorUtils::AddMemberVariable` using the §3.2 pin-type forward map — the forward twin of the `PinTypeToString` reverse map blueprint_get uses; `type` token: primitive `bool`/`int`/`int64`/`byte`/`float`/`string`/`name`/`text`, math struct `vector`/`vector2d`/`rotator`/`transform`/`color`, or a resolvable object/struct path; optional `is_array` wraps the type in an array; optional `default_value` (UE text format) is stored on the variable descriptor and only takes effect after a compile lands the property on the generated class; guards reject cross-namespace name collisions — member-variable names, SCS component names, and inherited parent properties all share the generated class's property namespace (`name_collision`) — and unresolvable type tokens (`invalid_type`); modify_variable renames and/or retypes with validate-before-mutate ordering — the rename is validated (well-formed + no collision) BEFORE the retype commits, because `ChangeMemberVariableType` commits immediately and `RenameMemberVariable` is void with no collision check of its own, so a colliding/ill-formed `new_name` would otherwise leave a partial retype; at least one of `new_name`/`new_type` required; set_default writes a Class Default Object property via the property's own text importer (`ImportText_Direct`) bracketed with `PreEditChange`/`PostEditChangeProperty` so an open Details panel / property-change observers refresh — changes the CLASS DEFAULT so it affects newly-spawned instances only, not placed actors (use `actor_modify` for a placed actor); compile-first — a member variable added via `blueprint_add_variable` is NOT a property on the generated class until `blueprint_compile` lands it, so set_default on such a property reports `property_not_found` with a message pointing at `blueprint_compile`; the expected loop is add → compile → set_default; all three mutate the asset, gate Enforce, `paths_hint` required) and `blueprint_add_function` / `blueprint_add_event` (P6.4 — Blueprint function / event graph STUBS: add_function creates an empty user function-graph stub via `FBlueprintEditorUtils::CreateNewGraph` + `AddFunctionGraph` — the K2 schema auto-wires the entry/result nodes and the MVP stub is parameter-less; the outer-name hijack guard probes `FunctionGraphs` + any `UObject` outered to the Blueprint because `CreateNewGraph` would otherwise resolve a name clash by renaming the EXISTING object aside (the EventGraph or any graph outered to the Blueprint) and silently report success — surfaced as `name_collision`; a null return is `create_graph_failed`; add_event enables or creates an overridable parent event node (e.g. `ReceiveBeginPlay` / `ReceiveTick`) via the K2 schema's `FunctionCanBePlacedAsEvent` + `FKismetEditorUtilities::AddDefaultEventNode` — the two-pronged resolution mirrors the K2 editor: a fresh Actor event graph is pre-seeded with DISABLED ghost nodes for the common events (INERT — they do NOT fire until enabled), so an existing disabled ghost is the node the tool ENABLES (enabling the ghost IS the "add event" op — never a false no-op), an already-enabled node is a real duplicate (`event_already_exists`), and no existing node → `AddDefaultEventNode` mints + the tool enables a fresh one; a `name` that names no parent `UFunction` OR names a non-`BlueprintEvent` function (e.g. `K2_DestroyActor`) is rejected by the schema check with `not_an_event`; no event graph → `no_event_graph`; STUB-ONLY scope — body authoring (`add_node` / `connect_pins` / free-form wiring) is OUT OF SCOPE for both tools; both mutate the asset, mark the Blueprint structurally modified, gate Enforce, `paths_hint` required) and `blueprint_compile` (P6.5 — Blueprint compile: compiles a Blueprint via `FKismetEditorUtilities::CompileBlueprint` with a silent `FCompilerResultsLog` (`bSilentMode = true` so the Output Log is not flooded on each compile) and returns a STRUCTURED error/warning list — the AI feedback loop; walks `Results.Messages` and maps each `FTokenizedMessage` to `{ severity ('error' / 'warning' / 'info'), message, node, graph }` with best-effort node/graph attribution extracted from the compiler's `UObject` tokens (a message with no graph context leaves `node`/`graph` empty — the common case for type/property-level diagnostics); result `{ succeeded, numErrors, numWarnings, messages[] }` where `succeeded` reflects the compiler's error count; a FAILED compile is a NORMAL, expected result, NOT a transport failure — the envelope stays `ok:true` and the result carries `succeeded:false` + the populated `messages[]` so an agent reads the diagnostics, fixes the structure via the add/modify tools, and recompiles (this is the contract that lets an agent treat "compile failed" as data rather than an opaque `isError`); only tool-level errors map to `ok:false` — `missing_parameter` (no path), `blueprint_not_found` (no Blueprint at path), `invalid_content_root` (Blueprint under `/Engine`/`/Script`/`/Temp`), `invalid_parameter` (malformed body); a non-zero compiler error count is NOT one of these codes (it rides through as `succeeded:false`); mutating — compile rebuilds the generated class + bytecode and dirties the package, gate Enforce, `paths_hint` required; under gate Enforce a failed compile may also surface verify `compile_errors` in the `gate` block — desirable (same package, same diagnostics) — pass `gate:"warn"` or `gate:"off"` for a tight recompile loop that always returns the `result`) and `blueprint_spawn` (P6.6 — Blueprint spawn: instances a compiled Actor Blueprint's `GeneratedClass` into the current editor level via `UWorld::SpawnActor` (headless-safe — NOT the viewport-aware `UEditorActorSubsystem::SpawnActorFromClass` that crashes under `-nullrhi` / Automation); closes the create → edit → compile → spawn loop (the Phase 6 exit gate); compile-first — a Blueprint with no `GeneratedClass` reports `not_compiled` (run `blueprint_compile` first), a non-Actor Blueprint reports `not_actor_blueprint`; optional `location` `{x,y,z}` (default origin) + `name` (actor label, de-duplicated via `SetActorLabelUnique`); rotation fixed at identity in the MVP; wrapped in `FScopedTransaction` for editor Undo, marks the level package dirty; result `{ actor (label), name, class, path, location:{x,y,z} }`; mutating, gate Enforce, `paths_hint` required; error codes: `missing_parameter` / `blueprint_not_found` / `not_compiled` / `not_actor_blueprint` / `no_editor_world` / `spawn_failed` / `invalid_parameter`) plus the Source read/list family shipped: `source_read` / `source_list` (read-only inspection + inventory of project C++ under `<Project>/Source/`; `source_read` — `path` relative to Source/ or absolute-inside, optional 1-based `start_line`/`end_line` window + a soft `max_lines` cap (default 2000, ceiling 20000), returns `{ path, total_lines, start_line, end_line, truncated, lines:[{line,text}] }`; `source_list` — optional `module` scope, `recursive` (default true), `extensions` allow-list (default `.h/.hpp/.c/.cc/.cpp/.cs`), returns `{ root, files:[{path,bytes}], count, total_bytes }`; both read-only, gate-free; every path access JAILED to `<Project>/Source/` via `ResolveJailedPath` — `..`, absolute-outside, and NTFS alternate-data-stream (`:`) escapes return `path_escapes_jail` and never read; the jail targets the PROJECT `Source/`, not `Plugins/UnrealOpenMCP`; the shared jail helpers `GetProjectSourceRoot` / `ResolveJailedPath` are the spine every later source sub-plan (CRUD / compile) builds on) plus the Source CRUD family shipped: `source_create_class` / `source_update` / `source_delete` (mutating, gate Enforce, `paths_hint` required; `source_create_class` — scaffold a header + cpp from parent-kind templates (UObject default / Actor / ActorComponent / None) into an existing module folder; `class_name` is the bare name without the U/A/F prefix (the prefix is derived from `parent_class`); `class_name` / `module` must be legal C++ identifiers (`invalid_parameter` otherwise — a generated class name can never inject C++ syntax); emits the `<MODULE>_API` dllexport macro + `GENERATED_BODY()` + the `<class>.generated.h` include for the UCLASS forms; refuses overwrite unless `force:true` (`already_exists`); a failed cpp write rolls back the just-written header so a stranded half-scaffold never blocks a clean retry without `force`; `module` defaults to the project's primary module and MUST already exist as a folder under Source/ (`module_not_found` — no Build.cs / module authoring in this phase); result `{ class_name (with derived prefix), module, parent_class (or '(none)'), header, cpp, is_uclass }`; `source_update` — full-file replace or a 1-based inclusive `start_line`/`end_line` line-range splice on an existing file (a half-range — only one of start_line/end_line — is rejected with `invalid_parameter`); preserves the detected EOL (CRLF/LF) and trailing newline when splicing so a one-line splice is a one-line diff, not a whole-file EOL rewrite; drops the `ParseIntoArrayLines` phantom-trailing-empty artifact on BOTH the existing file and the replacement `content` so a newline-terminated `content` (the common AI-caller form) does not splice a spurious blank line; validates the range in int64 before narrowing to int32 (out-of-range → `invalid_line_range`, never a silent mis-edit — a huge value would otherwise wrap and splice the WRONG lines); writes are BOM-less UTF-8 (a splice on a UTF-8-BOM file drops the BOM); result `{ path, mode ('full'|'range'), bytes_written, lines_written }`; `source_delete` — delete a single file (refuses directories with `is_directory`, refuses missing with `file_not_found`); destructive and NOT undoable from MCP — source files are not UObject assets, so there is no `FScopedTransaction` / recycle bin; the gate checkpoint is the only safety net (pass `gate:"off"` only when you accept that); result `{ path, deleted:true }`; all three JAILED to `<Project>/Source/` via `ResolveJailedPath` — `..`, absolute-outside, and NTFS ADS (`:`) escapes return `path_escapes_jail` and never write; no auto-compile — an agent calls the compile tool after the scaffold + edits) shipped; rest TBD |
- MCP resources source: `mcp-server/src/resources/` (when shipped)
- E2E smoke sources: `mcp-server/src/integration.test.ts` (in-process MCP client↔server over in-memory transport — ping, `actor_find`, `asset_find`, and Blueprint compile-loop round-trips), `mcp-server/scripts/p1-parity-smoke.mjs` / `p2-parity-smoke.mjs` / `p4-parity-smoke.mjs` / `p6-parity-smoke.mjs` (`npm run smoke:p1` / `smoke:p2` / `smoke:p4` / `smoke:p6`; scripted stdio smoke against the built `dist/index.js`; healthy / bridge-down / tool-error cases, plus the `smoke:p6` compile-failed = data case). See [E2E smoke verification](architecture.md#e2e-smoke-verification).

## Contract documentation guidance

- Prefer documenting behavior and payload shapes over implementation details.
- Call out breaking changes explicitly.
- Keep examples minimal and representative.
- Unreal content paths use `/Game/...`; document live/offline/local/batch routes per tool.

## Update triggers

Update this index when:

- a new API/protocol doc is added,
- contract ownership moves to new modules,
- endpoint or resource domains are reorganized.
